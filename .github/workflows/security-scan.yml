name: 🔒 Security Vulnerability Scan

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  schedule:
    # Ejecutar todos los lunes a las 9:00 AM UTC
    - cron: '0 9 * * 1'
  workflow_dispatch: # Permite ejecución manual

jobs:
  dependency-scan:
    name: 📦 Dependency Vulnerabilities
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: 📦 Install dependencies
        run: flutter pub get

      - name: 🔍 Run pub outdated (vulnerabilities check)
        run: |
          echo "## 📊 Dependency Security Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          flutter pub outdated --json > outdated.json || true
          if [ -s outdated.json ]; then
            echo "✅ Dependency scan completed" >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`json" >> $GITHUB_STEP_SUMMARY
            cat outdated.json | head -50 >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ No outdated packages found" >> $GITHUB_STEP_SUMMARY
          fi
        continue-on-error: true

      - name: 🛡️ Dependency Review (PRs only)
        if: github.event_name == 'pull_request'
        uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: moderate
          allow-licenses: MIT, Apache-2.0, BSD-2-Clause, BSD-3-Clause, ISC

  code-analysis:
    name: 🔎 Static Code Analysis
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: 📦 Install dependencies
        run: flutter pub get

      - name: 🔍 Flutter Analyze
        run: |
          echo "## 🔎 Static Analysis Report" >> $GITHUB_STEP_SUMMARY
          flutter analyze > analyze_output.txt 2>&1 || true
          
          if grep -q "No issues found!" analyze_output.txt; then
            echo "✅ No issues found!" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ Issues detected:" >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
            cat analyze_output.txt >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          fi

      - name: 🧪 Dart Code Metrics (Optional)
        run: |
          # Instalar herramienta de métricas
          flutter pub global activate dart_code_metrics
          
          echo "## 📈 Code Quality Metrics" >> $GITHUB_STEP_SUMMARY
          flutter pub global run dart_code_metrics:metrics analyze lib --reporter=console > metrics.txt 2>&1 || true
          
          if [ -s metrics.txt ]; then
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
            cat metrics.txt | head -100 >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          fi
        continue-on-error: true

  secret-scan:
    name: 🔐 Secret & Credential Scan
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Necesario para escanear todo el historial

      - name: 🔍 TruffleHog Secret Scan
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          base: ${{ github.event.repository.default_branch }}
          head: HEAD
          extra_args: --only-verified

      - name: 🔐 Gitleaks Secret Detection
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_LICENSE: ${{ secrets.GITLEAKS_LICENSE }}

  android-security:
    name: 🤖 Android Security Scan
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🔧 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: 📦 Install dependencies
        run: flutter pub get

      - name: 🏗️ Build Android APK (debug)
        run: flutter build apk --debug
        continue-on-error: true

      - name: 🔍 Android Lint Check
        run: |
          cd android
          ./gradlew lint || true
          echo "## 🤖 Android Lint Report" >> $GITHUB_STEP_SUMMARY
          
          if [ -f "app/build/reports/lint-results.xml" ]; then
            echo "✅ Lint scan completed" >> $GITHUB_STEP_SUMMARY
            echo "Check the artifacts for detailed report" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ Lint report not generated" >> $GITHUB_STEP_SUMMARY
          fi
        continue-on-error: true

      - name: 📤 Upload Lint Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: android-lint-report
          path: android/app/build/reports/
          retention-days: 30
        continue-on-error: true

  security-summary:
    name: 📋 Security Summary
    runs-on: ubuntu-latest
    needs: [dependency-scan, code-analysis, secret-scan, android-security]
    if: always()
    
    steps:
      - name: 📊 Generate Security Summary
        run: |
          echo "# 🔒 Security Scan Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Check | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Dependency Scan | ${{ needs.dependency-scan.result == 'success' && '✅ Passed' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Code Analysis | ${{ needs.code-analysis.result == 'success' && '✅ Passed' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Secret Scan | ${{ needs.secret-scan.result == 'success' && '✅ Passed' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Android Security | ${{ needs.android-security.result == 'success' && '✅ Passed' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🕐 Scan completed at: $(date -u)" >> $GITHUB_STEP_SUMMARY
